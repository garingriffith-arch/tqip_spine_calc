# Static assets

Optional static files for the Shiny app live here.

Recommended optional file:

```text
ohsu_logo.png
```

If `ohsu_logo.png` is present, the app header will display it. Otherwise, a text fallback is used.
